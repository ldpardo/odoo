<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="qr.list">
      <field name="name">qr list</field>
      <field name="model">qr.qr</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="qr.action_window">
      <field name="name">qr window</field>
      <field name="res_model">qr.qr</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="qr.action_server">
      <field name="name">qr server</field>
      <field name="model_id" ref="model_qr_qr"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="qr" id="qr.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="qr.menu_1" parent="qr.menu_root"/>
    <menuitem name="Menu 2" id="qr.menu_2" parent="qr.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="qr.menu_1_list" parent="qr.menu_1"
              action="qr.action_window"/>
    <menuitem name="Server to list" id="qr" parent="qr.menu_2"
              action="qr.action_server"/>
    -->
  </data>
</odoo>